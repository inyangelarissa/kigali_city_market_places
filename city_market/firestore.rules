rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary baseline: allow only authenticated users.
    // This removes permission errors while app behavior is stabilized.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
